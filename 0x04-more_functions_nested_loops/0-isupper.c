#include "main.h"
#include <stdio.h>

/**
 * main-function declaration
 *
 *Description: a function that check if a letter is upper case
 *
 *Return: always 0 (success)
*/
int _isupper(int c) {
	return (c >= 'A' && c <= 'Z');
}
